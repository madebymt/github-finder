import React from 'react'

const Alert = ({alert}) => {
    return ()
}

export default Alert
// alert !== null(     <div className={`alert ${alert.type}`}>         <i
// class="fas fa-inbox-in"></i><p>{alert.msg}</p>     </div> )